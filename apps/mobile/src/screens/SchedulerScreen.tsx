import { View, Text } from 'react-native';

export default function SchedulerScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Scheduler (Chat)</Text>
    </View>
  );
}
